# Current State

Shared **active** snapshot. Update when shared execution state changes (PR or `/agent-memory sync`). Versions, completed work, and roadmaps belong in canonical sources or Git — not here.

## In progress

High-level work only (detail in `active-work/<branch>.md`). Hooks refresh this list on session start; refine summaries when branch goals change:

- [`active-work/<branch>.md`](./active-work/) — _branch goal in one line._

## Blockers / attention

Shared impediments or memory↔canonical divergences. Per-branch blockers stay in active-work.

- _none_

## Handoff

Next action only when explicit/evidenced. Do not invent a roadmap.

- _none_
