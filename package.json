{
  "name": "@dosx/agent-memory",
  "version": "0.0.15",
  "description": "Local Markdown workspace memory for AI coding agents (Cursor, Claude Code, Codex, OpenCode, Copilot, Gemini). npx CLI installs the skill into .agents/skills/ and harness lifecycle hooks — no server, vector DB, or embeddings.",
  "bin": {
    "agent-memory": "bin/cli.js"
  },
  "scripts": {
    "build": "bun build ./install.ts --outfile bin/cli.js --target node --format cjs --banner '#!/usr/bin/env node' --minify",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "lint:md": "command -v markdownlint >/dev/null && markdownlint '**/*.md' --ignore node_modules --ignore bin || printf 'skip: markdownlint not installed\\n'",
    "build:check": "bun build ./install.ts --outfile /tmp/agent-memory-cli-check.js --target node --format cjs --banner '#!/usr/bin/env node' --minify && cmp -s bin/cli.js /tmp/agent-memory-cli-check.js",
    "test": "bash tests/reference-first-contract.sh && bash tests/hooks-checkpoint.sh && bash tests/version-parity.sh && bash tests/hooks-merge.sh && bash tests/cli-install.sh && bash tests/lockfile-only.sh && bash tests/migration-smoke.sh",
    "check": "bun run typecheck && bun run lint:md && bun run test && bun run build && bun run build:check",
    "prepublishOnly": "bun run build"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "files": [
    "bin/",
    "hooks/",
    "skills/agent-memory/"
  ],
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/diegoos/agent-memory.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "bun-types": "^1.3.14",
    "typescript": "5.8.3"
  }
}
