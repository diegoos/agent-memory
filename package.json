{
  "name": "@dosx/agent-memory",
  "version": "0.2.1-rc.3",
  "description": "Local Markdown workspace memory for AI coding agents (Cursor, Claude Code, Codex, OpenCode, Copilot, Gemini). npx CLI installs the skill into .agents/skills/ and harness lifecycle hooks — no server, vector DB, or embeddings.",
  "bin": {
    "agent-memory": "bin/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outfile bin/cli.js --target node --format cjs --banner '#!/usr/bin/env node'",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "lint:md": "bunx markdownlint '**/*.md' --ignore node_modules --ignore bin --ignore QA",
    "build:check": "bash scripts/build-check.sh",
    "test": "bash tests/test-runner.sh",
    "check": "bun audit && bun run typecheck && bun run lint:md && bun run build:check && bun run test && bun run build",
    "prepack": "bun audit && bun run build:check",
    "prepublishOnly": "bun run check"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "files": [
    "bin/",
    "hooks/",
    "skills/agent-memory/",
    "SECURITY.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/diegoos/agent-memory.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.4.1",
    "bun-types": "^1.4.1",
    "markdownlint-cli": "0.49.1",
    "typescript": "7.0.2"
  }
}
